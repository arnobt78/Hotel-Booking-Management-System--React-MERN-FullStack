# Netlify configuration for SPA routing
[build]
  publish = "hotel-booking-frontend/dist"
  command = "npm run build --prefix hotel-booking-frontend"

# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
